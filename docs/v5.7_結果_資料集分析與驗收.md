# v5.7 結果：資料集分析與驗收

| | |
| --- | --- |
| 日期 | 2026-09-13 |
| 與 v5.6 的差別 | **只有 `Thrips_Damage` 的 208 張重標**（一張葉子一個框）。其餘八類的來源逐位元相同 |
| 兩個臂 | `base`（對照）與 `ext`（train 多 124 張外部潛葉蛾影像，配額中性） |
| 驗收 | **兩臂都是八道 Gate 全綠（各 92 項）**，另加 v5.7 專屬的 Gate 0+ 三項 |
| 產出 | `Datasets/2_處理與切分/v5.7`、`Datasets/2_處理與切分/v5.7_ext` |

---

## 1. 為什麼有 v5.7

`Thrips_Damage` 的 valid/test 矛盾三輪都沒收斂（0.285 → 0.190 → 0.202）。
工作包 A 量出根因：**框本身就不一致**。舊約定下兩人的中位 IoU 只有 0.746，
改成「一張葉子一個框」之後升到 0.899（判準 0.85）。
完整經過見 [v5.6_結果_工作包A標註一致性.md](v5.6_結果_工作包A標註一致性.md)。

v5.7 就是把那份新約定套回全類 208 張的結果。**除此之外什麼都沒動**——
切分規則、增強 profile、配額、seed 全部沿用 v5.6，這樣 `Thrips_Damage` 的變化
才能歸因到標註，而不是別的東西。

---

## 2. 組成

### 2.1 來源樹

`Datasets/1_原始影像/v5.7/`，是 v5.6 的完整複本，只換掉
`Pests/Thrips_v5r.yolo26/train`：

| | v5.6 | v5.7 |
| --- | ---: | ---: |
| 影像 | 762 | 762（**內容逐位元相同**，其中 15 張被 Roboflow 改了檔名） |
| `Thysanoptera`（子類 0） | 837 框 / 554 張 | **837 框 / 554 張，逐字未動** |
| `thirps_leaf_damage`（子類 1） | 245 框 / 208 張 | **215 框 / 208 張** |
| 框中位面積 | 11.30% | **15.40%**（1.36 倍） |

208 張裡 **152 張座標完全沒改**（已符合新約定），**56 張有改**：
其中 **18 張是多框併成一框**（共少 30 框），**38 張框數不變、重畫了座標**（37 張是單框重畫）。
（2026-09-15 更正：原寫「56 張是多框併成一框」；以影像 md5 對應 v5.6／v5.7 標註逐檔比對後為上述組成。）仍有 6 張保留兩個框（T_034 / T_040 / T_064 / T_078 / T_091 / T_128）——
那是一張照片裡兩片葉子各一個框，**符合**「一葉一框」。

> 15 張改名是因為工作包 A 的 20 張本來就取自這批來源，重標時被上傳到同一個
> Roboflow 專案而去重。對照關係記在
> `Datasets/1_原始影像/_人工標註待辦/A_薊馬葉害_框怎麼畫/_原始檔名對照.csv`，
> 驗收時以**內容 md5** 比對，證實沒有影像遺失或重複。

### 2.2 產出

| | v5.6 | v5.7 base | v5.7 ext |
| --- | ---: | ---: | ---: |
| train 影像 | 7,264 | 7,264 | 7,264 |
| valid / test 影像 | 401 / 401 | 401 / 401 | 401 / 401 |
| train 框 | 18,078 | 18,046 | 18,048 |
| valid 框 | 868 | 863 | 863 |
| test 框 | 822 | 812 | 812 |
| 其中 `Thrips_Damage`（train / valid / test） | 313 / 47 / 51 | **281 / 42 / 41** | 281 / 42 / 41 |

**張數三個版本完全一樣**，差別只在框數。這是刻意的：ext 臂是**配額中性**的——
124 張外部影像佔用 `Citrus_Leaf_Miner` 既有的增強配額
（base 是 111 原始 + 333 增強 = 444；ext 是 111 原始 + 124 外部 + 209 增強 = 444），
所以 base 與 ext 可以直接對比，不需要考慮「train 變大了」這個混淆因子。

> 因為張數相同，**光看張數分不出兩臂**。訓練 notebook 改成直接讀
> `_provenance.json` 的 `meta.version` 與 `meta.arm`，拿錯資料集會停在 Step.2，
> 而不是跑完 3.4 小時才發現。

---

## 3. 驗收

### 3.1 Gate 0+：只准 `thirps_leaf_damage` 變（v5.7 新增）

v5.6 的 Gate 0 有一項是「來源樹與 v5.5 位元一致」。v5.7 就是為了重標而存在的，
那個資料夾**本來就該不一樣**——原樣套用只會得到一個必然失敗的紅字，久了會被習慣性忽略。
所以 Thrips 那個資料夾改問一個**更嚴格**的問題：它是不是「只」按預期變了。

| 檢查 | 結果 |
| --- | --- |
| Thrips 影像集合與 v5.5 相同（以內容 md5 比對） | ✓ 762 / 762 |
| `Thysanoptera`（子類 0）逐字未動 | ✓ 837 → 837 框 |
| 只有 `thirps_leaf_damage`（子類 1）變動 | ✓ 245 → 215 框，56 個檔案 |

其餘九個來源資料夾仍走原本的位元比對（5,496 個檔案全過）。

### 3.2 八道 Gate

**兩臂各 92 項檢查全綠。** 重點幾項：

| Gate | 內容 | 結果 |
| --- | --- | --- |
| 2 | 近重複（dHash ≤ 6）跨 split | 九類皆 0 組 |
| 4 | pooled ±2SE 投影 | 九類全部 ≤ 0.10，最差 CLM 0.099（與 v5.6 相同——評估**張數**沒變） |
| 5 | 逐張框面積膨脹率 | 九類皆 **1.000**（v5.5 是 1.075–1.300） |
| 6 | train vs 評估集的分佈位移 | `Thrips_Damage` 中位框尺寸比 **1.00**、每圖框數比 0.99 |
| 7 | 切分的決定性 | 重跑後評估集成員完全一致 |

---

## 4. 建置過程中修掉的一個 bug

**`cv2.imread` 在非 ASCII 路徑下會靜默回傳 `None`。**

第一次建完 v5.7，train 只有 2,673 張（應為 7,264），每一類都是「增強 0/570」。
本專案的來源樹路徑是 `Datasets/1_原始影像/...`（中文），OpenCV 5.0.0 的 `imread`
走 Windows 的窄字元 API，讀不到就回 `None` 而**不丟例外**；
`write_augmented()` 把 `None` 當成「這張跳過」，於是整批增強產出 0 張，
而建置仍然「成功」結束。

**而且八道 Gate 會全綠**——它們檢查的是比例（增強圖與來源圖的框面積比之類），
不是絕對張數。一份沒有任何增強的資料集可以一路過關到訓練。

處理：

1. `build_dataset_v5r.py` 新增 `imread_any()` / `imwrite_any()`
   （`np.fromfile` + `cv2.imdecode` / `cv2.imencode` + `write_bytes`），
   `build_dataset_v5_6.py`、`copy_paste_clm.py` 的呼叫全部改掉。
2. **逐像素驗證 `imdecode` 一樣會套用 EXIF 方向**（與 `PIL.ImageOps.exif_transpose`
   的輸出完全相同），所以換過去不會改變任何既有資料的座標系。
3. 加保險：**任何類別配額不為 0 卻產出 0 張增強圖，當場中止**。

> `Datasets/2_處理與切分/v5.6` 現有的 7,264 張是路徑改版前建的，沒有受影響。
> 但今天任何人重建 v5.6 都會踩到同一個坑。

---

## 5. 已知限制

1. **`Thrips_Damage` 的 AP 不能與 v5.6 直接比。** 框的定義換了、中位面積變成 1.36 倍、
   框數從 245 降到 215。能比的是「valid/test 還矛不矛盾」，不是 AP 的絕對值。
   其餘八類的來源逐位元相同，可以比。

2. **重標後的類內一致性是 0.80–0.83，不是 0.899。**
   0.899 是「兩個人當場照新約定重畫 20 張」的水準；實際重標時 **152 / 208 張沿用了歷史框**
   （它們本來就只有一個框、已符合新約定，重畫的成本不會換到東西）。
   以工作包 A 的 20 張當基準實測，這批新標註對第一位是 0.832、對第二位 0.800。
   分開看，**沒動過的 13 張是 0.849、有改過的 7 張是 0.749**（兩位標註者合併）——沒動過的並沒有比較差，
   （2026-09-15 更正：原寫「12 張 0.837／8 張 0.784」，逐檔重算後為上述數字）
   所以問題不在「152 張沒重畫」。差距集中在兩個已知原因：
   細長框的 IoU 天生吃虧、以及「多淡還算受害」沒有定義
   （見 [工作包 A 結果](v5.6_結果_工作包A標註一致性.md) §9.3）。**要寫進研究限制。**

3. **`build_dataset_v5_6.py` 第 233 / 268 行用 `PILImage.open(img).size` 取寬高，
   這個不套 EXIF。** Thrips 的 208 張葉害圖裡有 56 張帶旋轉標記，
   對它們會拿到對調的寬高。影響的是**框尺寸統計**（Gate 5/6 的比值、v5.6 §5 那類數字），
   不影響訓練資料本身——寫出影像走的是 `imread_any()`，會套 EXIF，與標註同一個座標系。
   `Aphid` 的 `equiv_px` 過濾只作用在 cid 7，薊馬不受影響。**未修，列為已知限制。**

4. **v5.7 的來源樹是 v5.6 的完整複本**（2.1 GB），不是用連結。
   好處是 v5.6 完全不受影響、可重現；代價是磁碟多佔 2.1 GB。

---

## 6. 下一步：兩臂訓練

| | RUN | Kaggle slug | 資料集 |
| --- | --- | --- | --- |
| 對照 | `v5.7_v11_5` | `datasets-yolo26-v5-7` | `Datasets/2_處理與切分/v5.7` |
| 實驗 | `v5.7ext_v11_5` | `datasets-yolo26-v5-7-ext` | `Datasets/2_處理與切分/v5.7_ext` |

notebook 由 `tools/make_train_notebooks_v5_7.py` 從 `train_v11_5.ipynb` 生成，
**16 個 cell 裡有 13 個逐字沿用**，只有標題、設定、與 Step.2 的指紋不同。
用生成的而不是手抄兩份，是因為「兩臂的訓練程式完全相同」正是這個 A/B 能成立的前提。

### 判準（先寫死，再看結果）

| # | 問題 | 判準 |
| --- | --- | --- |
| 1 | 外部影像對 CLM 有沒有幫助 | ext 的 `Citrus_Leaf_Miner` AP50 減 base，要**超過 2σ**（逐類雜訊地板 ±0.04）才算數 |
| 2 | 重標有沒有修好 `Thrips_Damage` 的矛盾 | valid − test 的差距要掉到**顯著門檻 0.165 以內**（v11.5 是 0.202） |
| 3 | 其餘七類 | **不預期任何變化**；超過 ±0.04 才需要解釋 |

> ext 臂的 124 張只來自約 20–25 片不同的葉子（B 包的 150 張有大量連拍），
> 所以即使 #1 過了，也要記得它證明的是「這 20 幾片葉子有用」，不是「外部資料普遍有用」。
